import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "Science — HEIKI HEIKI Pro" };

export default function Page() {
  return (
    <PlaceholderPage
      pathname="/science"
      title="Science"
      description="The thinking behind the formulas. Method notes, references, and what we do not claim."
    />
  );
}
